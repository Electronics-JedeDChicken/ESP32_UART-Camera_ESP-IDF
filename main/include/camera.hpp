// .hpp is like the "menu," includes & inits but no implementation (just declaration)
// #pragma once- prevents same header from being included multiple times

#include "esp_camera.h"

esp_err_t cameraInit();